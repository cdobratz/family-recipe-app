name: CI/CD Pipeline

on:
  push:
    branches: [ main, development, family ]
  pull_request:
    branches: [ main, development, family ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest
    - name: Run tests
      run: |
        pytest

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8
    - name: Run linting
      run: |
        flake8 . --count --max-complexity=10 --max-line-length=127 --statistics

  deploy-family:
    needs: [test, lint]
    if: github.ref == 'refs/heads/family' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Deploy to family environment
      run: |
        echo "Add your family deployment steps here"
        # Add actual deployment steps based on your hosting solution
